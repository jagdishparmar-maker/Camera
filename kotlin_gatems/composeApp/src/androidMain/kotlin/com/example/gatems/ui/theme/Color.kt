package com.example.gatems.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary (lime green — same as Expo #8CE02A) ──────────────────────────────
val Primary             = Color(0xFF8CE02A)
val PrimaryContainer    = Color(0xFF5A8A1A)
val OnPrimary           = Color(0xFF1A1A1A)
val OnPrimaryContainer  = Color(0xFFC8FF6A)

// ── Secondary (muted purple) ──────────────────────────────────────────────────
val Secondary            = Color(0xFFE0C8FF)
val SecondaryContainer   = Color(0xFF5A3A8A)
val OnSecondary          = Color(0xFF1A1A1A)
val OnSecondaryContainer = Color(0xFFE8D8FF)

// ── Tertiary (light blue accent) ─────────────────────────────────────────────
val Tertiary            = Color(0xFF7DD3FC)
val TertiaryContainer   = Color(0xFF0C4A6E)
val OnTertiary          = Color(0xFF1A1A1A)
val OnTertiaryContainer = Color(0xFFBAE6FD)

// ── Surfaces & backgrounds ────────────────────────────────────────────────────
val Background      = Color(0xFF2C2C2E)
val OnBackground    = Color(0xFFFFFFFF)
val Surface         = Color(0xFF3A3A3C)
val SurfaceVariant  = Color(0xFF48484A)
val OnSurface       = Color(0xFFFFFFFF)
val OnSurfaceVariant= Color(0xFFC0C0C0)

// ── Outlines ──────────────────────────────────────────────────────────────────
val Outline         = Color(0xFF636366)
val OutlineVariant  = Color(0xFF48484A)

// ── Error ─────────────────────────────────────────────────────────────────────
val Error           = Color(0xFFFF453A)
val OnError         = Color(0xFFFFFFFF)
val ErrorContainer  = Color(0xFF7A1A1A)

// ── Status chip colours (mirrors STATUS_COLORS in Expo) ──────────────────────
val StatusCheckedIn  = Color(0xFF22C55E)
val StatusCheckedOut = Color(0xFF64748B)
val StatusDockedIn   = Color(0xFF3B82F6)
val StatusDockedOut  = Color(0xFFF59E0B)

val StatusTextCheckedIn  = Color(0xFF0F172A)
val StatusTextCheckedOut = Color(0xFFFFFFFF)
val StatusTextDockedIn   = Color(0xFFFFFFFF)
val StatusTextDockedOut  = Color(0xFF0F172A)
