# Legacy Expo Archive

This folder contains the archived Expo codebase after migration to Kotlin (`kotlin_gatems`).

## Archived path

- `legacy/expo-app/`
  - `app/`
  - `components/`
  - `contexts/`
  - `hooks/`
  - `lib/`
  - `assets/`
  - `root-config/` (Expo root config files)

## Notes

- Active app development now targets `kotlin_gatems/`.
- If needed, restore any archived file by moving it back from `legacy/expo-app/`.
